@model front_end_mvc_dot_net_framework.Models.Tools
@{
    ViewBag.Title = "AddOrUpdate";
}

<div class="container">
    @using (Html.BeginForm())
    {
        @Html.HiddenFor(model => model.id)
        @Html.HiddenFor(model => model.id)
        <div class="form-group">
            @Html.LabelFor(model => model.partName)
            @Html.EditorFor(model => model.partName)
            @Html.ValidationMessageFor(model => model.partName)
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.partType)
            @Html.EditorFor(model => model.partType)
            @Html.ValidationMessageFor(model => model.partType)
        </div>

        <div class="form-group">
            <input class="btn btn-default" type="submit" name="tool" value="Submit" />
            <input class="btn btn-default" type="submit" name="tool" value="Clear" />
        </div>
    }
</div>

