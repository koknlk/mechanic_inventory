@model IEnumerable<front_end_mvc_dot_net_framework.Models.ToolCheckOut>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>



<p>
    <a href="@Url.Action("CheckOut", "ToolsCheckOut")" class="btn btn-default">Check Out Tool</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.mechanic)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.partName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.partType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.mechanic)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.partName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.partType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.date)
            </td>
            <td>
                <a class="btn btn-default" onclick="Remove()">Remove</a>
            </td>
        </tr>
    }

</table>

<script>
    function Remove() {
        confirm("Mechanc Employee", "Are you sure?", () => {
            window.location.href = '@Url.Action("Remove", "ToolsCheckOut")/' + id;
        }, null)
    }
</script>